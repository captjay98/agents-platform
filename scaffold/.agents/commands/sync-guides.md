---
description: 'Sync agent guides with current project state'
---

@fullstack-engineer Sync agent guides: $ARGUMENTS

## Protocol

1. Rebuild AGENTS.md:
```bash
# <!-- PROJECT: Your build command, e.g. node .agents/scripts/build.mjs -->
```
2. Review `.agents/guide-header.md` — does it reflect current architecture?
3. Review `.agents/memory/project-memory.md` — any stale info?
4. Check personas reference current tech stack and patterns.
