# Neon Database

<!-- PROJECT: Your Neon setup. Example:

## Project
- Project ID: [id]
- Region: [region]

## Environments
- `main` branch → production
- `dev` branch → development
- Feature branches → ephemeral (auto-delete after merge)

## Migrations
- Tool: Kysely / Laravel / TypeORM migrations
- Run: `bun run db:migrate:up` / `php artisan migrate`
-->

## Conventions

- Neon MCP is for inspection only, not schema changes
- Always test migrations on a branch before main
- Monitor connection pool usage
