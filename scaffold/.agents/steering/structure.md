# Project Structure

<!-- PROJECT: Your directory layout. Example:

```
├── app/                    # Application code
│   ├── features/           # Feature modules
│   │   ├── auth/
│   │   ├── orders/
│   │   └── payments/
│   ├── lib/                # Shared utilities
│   └── routes/             # Route definitions
├── database/
│   └── migrations/
├── public/
├── tests/
└── .agents/                # Agent configuration
```

## Key Directories
- `app/features/` — Each feature is self-contained (routes, components, services)
- `app/lib/` — Shared code (db, auth, utils)
- `database/migrations/` — Schema changes

## Conventions
- Feature-first organization, not layer-first
- Co-locate tests with source files
- One export per file for components
-->
